# Autodream — 2026-05-25

> Synthetic example. Numbers, session IDs, and project names below are illustrative —
> they show the shape and granularity of a real overnight report. Real reports are
> denser and more specific to your sessions.

## Activity snapshot

- **42 session JSONs** read (2 empty `.err` files — both 0 bytes, treated as silent successes).
- ~11 projects touched; top 3 by session count: `personal-scratch` (triage workers + scratch, ~18), `web-app` (~5), `data-pipeline` / `infra-tools` / `docs-site` (3 each).
- Substantive sessions (>100 turns): 4 — `a1b2c3d4` (web-app, 1180 turns / 920 tools), `e5f6a7b8` (data-pipeline, 612 / 480), `9c0d1e2f` (docs-site PDF ingest, 410 / 0), `3a4b5c6d` (infra-tools queue design, 388 / 332).
- Aggregate (best-effort sum across reported JSONs): ~3500 turns, ~1800 tool calls.
- Skills invoked across the day: `commit-and-verify` ×2, `code-review` ×1, `handoff` ×1, `brainstorming` ×1. Most sessions invoked **zero** skills.
- Models: `claude-opus-4-7` for substantive work (4 sessions); `claude-haiku-4-5` for the ~16 Layer-1 triage workers.

## Top patterns (ranked)

### 1. Tool loop: Read on oversized JSONL files
- **Category**: tool_loop / sandbox_friction
- **Count**: 4 sessions (`a1b2c3d4`, `7e8f9a0b`, `b2c3d4e5`, `f6a7b8c9`)
- **Severity**: high
- **Examples**:
  - `f6a7b8c9`: "Read tool called 13 times sequentially with different offset/limit parameters… thinking blocks show: 'too large (4.5M tokens)'; 'try smaller chunks'; attempt sequence: limit=2000, offset sampling, limit=500, limit=100, limit=10."
  - `a1b2c3d4`: "Read… → Error: File content (512KB) exceeds maximum allowed size (256KB)" then "Read with limit=2000 → Error: File content (9857123 tokens) exceeds maximum allowed tokens (25000)."
  - `7e8f9a0b`: "Read tool repeatedly rejected large JSONL file (263.3KB)."
- **Proposed action**: Update the Layer-1 triage prompt to **start every triage worker with `wc -l` + `head -200` + `tail -50` via Bash** before any Read. Cap Read attempts at 1; pivot to `jq` / `sed` sampling on first token-limit error.
- **Confidence**: high
- **Auto-applied**: no — L2 performs no memory writes under OMP.

### 2. `commit-and-verify` skill never invoked despite mandate
- **Category**: missed_skill
- **Count**: 3 sessions across 2 projects (`a1b2c3d4` web-app, `9c0d1e2f` docs-site, `e5f6a7b8` data-pipeline)
- **Severity**: high
- **Examples**:
  - `a1b2c3d4`: "5+ git commits made via Bash… but skill was invoked 0 times. User's CLAUDE.md explicitly states 'ALWAYS use the commit-and-verify skill.'"
  - `9c0d1e2f`: "`git commit -m \"docs: link housekeeping pass\"` followed by git push, no skill invocation."
  - `e5f6a7b8`: "`git commit -m \"fix(ingest): handle malformed timestamps\"` via Bash; commit-and-verify never resolved."
- **Proposed action**: No memory pin applies — under OMP, L2 performs no memory writes. Memory alone could not fix this; a **PreToolUse hook** on `Bash(git commit *)` that hard-fails unless `commit-and-verify` ran in the same session is the concrete next step.
- **Confidence**: high
- **Auto-applied**: no — L2 performs no memory writes under OMP.

### 3. `dangerouslyDisableSandbox` overuse in `web-app`
- **Category**: sandbox_friction
- **Count**: 1 high-volume session (`a1b2c3d4`: 410/520 Bash calls = 79% bypass)
- **Severity**: high
- **Example**: "Permission denied errors on `./scripts/sync-content.sh`. Global instructions note 'sandbox blocks gh CLI' but no allowlist configured."
- **Proposed action**: Add a `permissions.allow` block to that project's `.claude/settings.json` for `Bash(gh:*)`, `Bash(./scripts/sync-content.sh:*)`, and the few other repeat offenders. The session is also a good candidate for the `fewer-permission-prompts` skill — point it at the transcript and let it propose an allowlist.
- **Confidence**: high
- **Auto-applied**: no — settings.json edit is human-controlled.

### 4. Missing `ASSUMPTIONS` blocks on non-trivial design work
- **Category**: assumption_unsurfaced
- **Count**: 2 sessions (`3a4b5c6d` infra-tools queue design, `a1b2c3d4` web-app feature design)
- **Severity**: high (3a4b5c6d), medium (a1b2c3d4)
- **Example** (`3a4b5c6d`): "User asked 'should we have a general queue instead of the voicemail group?' Claude proceeded with design decisions across 388 turns without surfacing any ASSUMPTIONS I'M MAKING block."
- **Proposed action**: Reinforce by triggering `superpowers:brainstorming` on session-open when the first message matches a design-question heuristic. Could be a UserPromptSubmit hook matching `/\b(should we|how should|let'?s design|what about)\b/i`.
- **Confidence**: medium (trigger heuristic needs tuning)
- **Auto-applied**: no.

### 5. `update-config` skill missed when editing `settings.json`
- **Category**: missed_skill
- **Count**: 1 (`d7e8f9a0`)
- **Severity**: high
- **Example**: "Claude manually edited `~/.claude/settings.json` by reading then Edit-ing it to add `cleanupPeriodDays: 9999`. Should have invoked update-config skill."
- **Proposed action**: Singleton — not yet a pattern. Note for future review; if it recurs once more, add a hook on Edit/Write whose path matches `settings.json` to suggest the `update-config` skill.
- **Confidence**: medium
- **Auto-applied**: no.

### 6. Triage worker wrote findings to wrong path (Layer-1 reliability)
- **Category**: incomplete_execution
- **Count**: 2 sessions (`b2c3d4e5` wrong path, `c4d5e6f7` never called Write at all)
- **Severity**: critical (`b2c3d4e5`), high (`c4d5e6f7`)
- **Examples**:
  - `b2c3d4e5`: "Prompt specified SESSION_PATH=…b2c3d4e5… but Claude read from …d7e8f9a0… (different file). Then wrote findings to the wrong output path."
  - `c4d5e6f7`: "Instructions clearly state 'Write to OUTPUT_PATH'. Session analyzed multiple files but never invoked the Write tool."
- **Proposed action**: Add two lines to the Layer-1 triage prompt: (1) echo `SESSION_PATH` and `OUTPUT_PATH` as the literal first line of output, (2) refuse to exit without calling Write at `OUTPUT_PATH`. Layer-2 should also verify each `*.json` filename matches its `session_path` basename and flag mismatches.
- **Confidence**: high
- **Auto-applied**: no — autodream-pipeline change.

## Per-project notes

- **web-app** (3 substantive findings, all from `a1b2c3d4`): heaviest project of the day. A single big-PR session (feature work + small refactor + 4 security fixes) lit up every signal — skill misses, sandbox bypass, no assumptions block. The 79% sandbox-bypass rate is the most actionable single fix.
- **data-pipeline** (2 sessions with findings): both substantive ingest work. The recurring miss is `commit-and-verify`. One session also did real engineering discovery (3 pre-existing defects in the timestamp parser) — net positive day, just commits the wrong way.
- **infra-tools** (`3a4b5c6d`): well-executed multi-option queue design, but no `ASSUMPTIONS` block and no `brainstorming` skill across 388 turns of design conversation. Skills that *were* used (`commit-and-verify`) suggest the operator knows the system — the miss is on creative-phase skills.
- **personal-scratch** (the triage worker pool): dominated by Layer-1 sessions wrestling with oversized JSONL inputs. Findings 1 and 6 are the relevant fixes. Most of these sessions are not "real" user work — they're autodream itself.

## Skill coverage gaps

Cross-checking the day's `missed_skill` findings against installed skills:

- `commit-and-verify` — installed. Repeat miss in `web-app` + `data-pipeline`. No coverage gap; enforcement gap. **Recommend a hook, not a new skill.**
- `update-config` — installed and accurately describes its trigger ("any changes to settings.json/settings.local.json"). Coverage exists; the miss in `d7e8f9a0` was a behavior gap.
- `brainstorming` — installed (`superpowers:brainstorming`). Description matches the `3a4b5c6d` use case. Behavior gap, not coverage.
- **No new skill needed.** Every recurring pattern from yesterday already has a skill that should fire.

## Triage failures

- `900c278c1fb1.json.err` — 0 bytes, no JSON file present alongside. Treating as "Layer-1 worker exited cleanly but emitted no JSON." Cannot recover.
- `fc2eeeb8badc.json.err` — same shape, 0 bytes, no JSON. Same disposition.
- Recommend the Layer-1 wrapper script touch the `.json` file with `{"findings":[]}` on success and only leave `.err` populated on actual stderr output, so silent drops are distinguishable from no-findings sessions.

## Open questions for the user

**Enforcement (skills miss):**
- Want a `PreToolUse` hook on `Bash(git commit *)` that hard-fails unless `commit-and-verify` ran in the same session? It would catch the recurring web-app + data-pipeline misses but would also bark on legitimate one-line docs commits — acceptable?

**Sandbox (web-app):**
- Ready to add `gh`, `./scripts/sync-content.sh` to that project's `.claude/settings.json` permissions? Currently 79% of Bash calls in that project bypass the sandbox.

**Autodream pipeline:**
- OK to update `SESSION_TRIAGE.md` to mandate (a) `wc -l` + `head` sampling before Read on JSONL, (b) echo of `SESSION_PATH`/`OUTPUT_PATH` at start, (c) refusal to exit without Write at `OUTPUT_PATH`? Findings 1 and 6 collapse if so.

## Auto-applied edits

None this run. The two high-confidence/high-severity recurring findings recur across projects and are reported in Top patterns for the human to act on; under OMP, L2 does not write memory, so no pin applies and none is added. All other actionable items fall outside the auto-edit scope (autodream scripts, global CLAUDE.md, `settings.json` permissions).
